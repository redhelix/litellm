---
phase: 04-config-drift-benchmark-runner
plan: 04
type: execute
wave: 3
depends_on:
  - 04-03
files_modified: []
autonomous: false
requirements:
  - DRIFT-01
  - DRIFT-02
  - DRIFT-03
  - DRIFT-04
  - BENCH-01
  - BENCH-02
  - BENCH-03

must_haves:
  truths:
    - "Config Drift section is visible in the dashboard with correct severity colour coding"
    - "Benchmark Runner section is visible with a working Run benchmark button"
    - "Full automated test suite is green before human verification begins"
  artifacts:
    - path: "dashboard/src/components/ConfigDriftView.tsx"
      provides: "Deployed and rendering in browser"
      exports: ["ConfigDriftView"]
    - path: "dashboard/src/components/BenchmarkRunner.tsx"
      provides: "Deployed and rendering in browser"
      exports: ["BenchmarkRunner"]
  key_links:
    - from: "browser"
      to: "http://docker-001:4002"
      via: "local network access"
      pattern: "docker-001:4002"
---

<objective>
Human verification checkpoint: confirm Phase 4 features are working end-to-end
on docker-001 before closing the phase.

Purpose: Automated tests cannot verify visual severity colours, live benchmark
firing against the real LiteLLM proxy, or streaming TTFT measurement. A brief
manual check closes that gap.

Output: Human sign-off that Config Drift and Benchmark Runner are working as
specified. No code changes in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-config-drift-benchmark-runner/04-UI-SPEC.md
@.planning/phases/04-config-drift-benchmark-runner/04-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full automated test suite — confirm GREEN baseline</name>
  <files></files>
  <action>
Run both test suites in sequence. Both must exit 0 before proceeding to the
human-verify checkpoint.

```bash
cd /home/rhx/projects/home-infra-backups/litellm/dashboard && npm run test -- --run
cd /home/rhx/projects/home-infra-backups/litellm/dashboard-sidecar && python -m pytest tests/ -q
```

If any test fails: stop here, fix the failure, re-run before proceeding.
Do NOT proceed to human verification with failing tests.
  </action>
  <verify>
    <automated>cd /home/rhx/projects/home-infra-backups/litellm/dashboard && npm run test -- --run && cd ../dashboard-sidecar && python -m pytest tests/ -q</automated>
  </verify>
  <done>Both test commands exit 0. Zero failures. All Phase 4 tests GREEN.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Config Drift section (DRIFT-01..04) and Benchmark Runner section (BENCH-01..03)
appended to the dashboard below the Trends section. The sidecar serves two new
endpoints: GET /api/config/diff and POST /api/benchmark/run.
  </what-built>
  <how-to-verify>
Open the dashboard at http://docker-001:4002 in a browser.

**Config Drift (scroll below Trends section):**
1. Confirm the "Config Drift" section heading is visible.
2. If the deployed config and repo config are currently identical, confirm
   "No differences detected" is shown (this is the correct baseline —
   configs were byte-identical as of 2026-04-13).
3. To test the security warning: temporarily edit /app/config.repo.yaml on
   docker-001 to add `general_settings: {master_key: sk-test-hardcoded}`,
   restart the sidecar, reload the dashboard. Confirm an orange Alert
   appears at the top of the diff list with title
   "Security: hardcoded master_key detected". Revert after testing.
4. Confirm mismatch rows show amber △ MISMATCH labels.
5. Confirm missing rows show red ✗ MISSING labels.

**Benchmark Runner (scroll below Config Drift):**
1. Confirm "Benchmark Runner" heading and "Run benchmark" button are visible.
2. Click "Run benchmark" — confirm the AlertDialog appears with title
   "Run benchmark across all models?" and a "Don't run" cancel button.
3. Click "Don't run" — confirm the dialog closes, no request is fired,
   button returns to idle.
4. Click "Run benchmark" again, then click "Run benchmark" in the dialog.
5. Confirm the button enters disabled state with "Running…" label.
6. Wait up to 60 seconds. Confirm the results table populates with per-model
   rows showing TTFT (ms), Latency (ms), tok/s, and ok/error/timeout badges.
7. Confirm the History section shows the completed run as "Run #1".
8. Click the history row — confirm the results table switches to that run.
9. Click "← Back to latest" — confirm latest results are restored.
  </how-to-verify>
  <resume-signal>
Type "approved" when all checks pass.
If issues are found, describe what is wrong and Claude will fix before
re-running verification.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human → dashboard UI | Read-only check (Config Drift) + single write action (benchmark trigger) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Tampering | Manual config edit for DRIFT-02 test | accept | Test edit is temporary and explicitly reverted; production config is not changed |
| T-04-04-02 | Denial of Service | Benchmark verification run | accept | Single test run; local network only; no external exposure |
</threat_model>

<verification>
Human sign-off captures what automated tests cannot verify:
- Visual severity colours (orange-500 security, amber mismatch, red missing)
- Live benchmark TTFT measurement via streaming response
- AlertDialog interaction (Don't run cancels cleanly)
- History expand/collapse state management
- 5-second poll termination on completion
</verification>

<success_criteria>
- Full automated test suite is GREEN before checkpoint
- Human confirms Config Drift section renders with correct severity colours
- Human confirms Benchmark Runner fires successfully and displays results
- Human confirms history expand/collapse works
- Human confirms "Don't run" cancels cleanly
- Human types "approved" to close Phase 4
</success_criteria>

<output>
After completion, create `.planning/phases/04-config-drift-benchmark-runner/04-04-SUMMARY.md`
</output>
