---
phase: 05-containerized-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yaml
  - .env.template
autonomous: true
requirements:
  - SYS-01
  - SYS-03

must_haves:
  truths:
    - "No secret or credential is hardcoded in docker-compose.yaml — every secret references a ${VAR} placeholder"
    - ".env.template documents every variable required to bring the stack up from scratch"
    - "The db service reads POSTGRES_PASSWORD from an env var, not a literal string"
    - "DATABASE_URL for both litellm-proxy and dashboard-sidecar is constructed from env vars, not a hardcoded connection string"
  artifacts:
    - path: "docker-compose.yaml"
      provides: "All secrets replaced with ${VAR} references"
      contains: "POSTGRES_PASSWORD=${POSTGRES_PASSWORD}"
    - path: ".env.template"
      provides: "Complete onboarding reference for all required env vars"
      contains: "LITELLM_BENCH_KEY"
  key_links:
    - from: "docker-compose.yaml db.environment"
      to: ".env POSTGRES_PASSWORD"
      via: "${POSTGRES_PASSWORD} substitution"
      pattern: "POSTGRES_PASSWORD=\\$\\{POSTGRES_PASSWORD\\}"
    - from: "docker-compose.yaml litellm.environment DATABASE_URL"
      to: ".env POSTGRES_PASSWORD"
      via: "postgresql://litellm:${POSTGRES_PASSWORD}@db:5432/litellm"
      pattern: "DATABASE_URL=postgresql://litellm:\\$\\{POSTGRES_PASSWORD\\}"
---

<objective>
Eliminate every hardcoded secret from docker-compose.yaml and make .env.template a complete onboarding document.

Purpose: SYS-01 requires no secret to be hardcoded in any committed file. The current docker-compose.yaml has the Postgres password (`litellm-synergy-2026`) hardcoded in three places — the db service, the litellm service, and the dashboard-sidecar service. Anyone with repo access sees the credential.

Output: A docker-compose.yaml where all secrets are ${VAR} references and a .env.template that lists every variable needed to start the stack cold.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hardcoded secrets in docker-compose.yaml with env var references</name>
  <files>docker-compose.yaml</files>
  <action>
Edit docker-compose.yaml to remove all hardcoded credential strings. Apply these exact substitutions:

1. db service — replace hardcoded POSTGRES_PASSWORD value:
   BEFORE: `- POSTGRES_PASSWORD=litellm-synergy-2026`
   AFTER:  `- POSTGRES_PASSWORD=${POSTGRES_PASSWORD}`

   Also parameterise POSTGRES_USER and POSTGRES_DB while touching this block (they are not secrets but should be consistent with DATABASE_URL construction):
   BEFORE: `- POSTGRES_USER=litellm`
   AFTER:  `- POSTGRES_USER=${POSTGRES_USER:-litellm}`
   BEFORE: `- POSTGRES_DB=litellm`
   AFTER:  `- POSTGRES_DB=${POSTGRES_DB:-litellm}`

2. litellm service — replace hardcoded DATABASE_URL:
   BEFORE: `- DATABASE_URL=postgresql://litellm:litellm-synergy-2026@db:5432/litellm?connect_timeout=10`
   AFTER:  `- DATABASE_URL=postgresql://${POSTGRES_USER:-litellm}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB:-litellm}?connect_timeout=10`

3. dashboard-sidecar service — replace hardcoded DATABASE_URL:
   BEFORE: `- DATABASE_URL=postgresql://litellm:litellm-synergy-2026@db:5432/litellm?connect_timeout=10`
   AFTER:  `- DATABASE_URL=postgresql://${POSTGRES_USER:-litellm}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB:-litellm}?connect_timeout=10`

Do NOT change any other lines. Do NOT change the PROMETHEUS_URL (not a secret — a LAN address).
Do NOT add LITELLM_MASTER_KEY to the dashboard-sidecar environment block — SYS-02 enforcement.
  </action>
  <verify>
    <automated>grep -n "litellm-synergy-2026" /home/rhx/projects/home-infra-backups/litellm/docker-compose.yaml; echo "Exit: $? (0 means secret still present — FAIL; 1 means clean — PASS)"</automated>
  </verify>
  <done>grep for the literal password string returns no matches (exit code 1). All three DATABASE_URL and POSTGRES_PASSWORD lines reference ${POSTGRES_PASSWORD}.</done>
</task>

<task type="auto">
  <name>Task 2: Bring .env.template to complete coverage of all required variables</name>
  <files>.env.template</files>
  <action>
The current .env.template is missing several variables that docker-compose.yaml requires. A developer cloning this repo and copying .env.template to .env must be able to bring the stack up with only their own API keys filled in.

Replace the entire .env.template with the following content (preserving all existing entries, adding missing ones, adding section comments for clarity):

```
# ── Postgres ─────────────────────────────────────────────────────────────────
POSTGRES_USER=litellm
POSTGRES_PASSWORD=REDACTED
POSTGRES_DB=litellm

# ── LiteLLM ──────────────────────────────────────────────────────────────────
# Master key for the proxy — used by litellm-proxy only (SYS-02: never add to dashboard-sidecar)
LITELLM_MASTER_KEY=REDACTED

# ── Dashboard sidecar ─────────────────────────────────────────────────────────
# Bench key must be a valid LiteLLM API key with permissions to call /chat/completions
LITELLM_BENCH_KEY=REDACTED

# ── External API keys ─────────────────────────────────────────────────────────
ANTHROPIC_API_KEY=REDACTED
OPENAI_API_KEY=REDACTED
OPENROUTER_API_KEY=REDACTED
PERPLEXITYAI_API_KEY=REDACTED
MOONSHOT_API_KEY=REDACTED
GEMINI_API_KEY=REDACTED
FIRECRAWL_API_KEY=fc-dummy-self-hosted

# ── Weights & Biases ──────────────────────────────────────────────────────────
WANDB_API_KEY=REDACTED
WANDB_PROJECT=litellm-proxy
```

This template is committed to the repo. It must never contain real values — only `REDACTED` placeholders so the reader knows the shape expected.
  </action>
  <verify>
    <automated>for var in POSTGRES_PASSWORD POSTGRES_USER POSTGRES_DB LITELLM_MASTER_KEY LITELLM_BENCH_KEY ANTHROPIC_API_KEY OPENAI_API_KEY WANDB_API_KEY; do grep -q "^${var}=" /home/rhx/projects/home-infra-backups/litellm/.env.template && echo "OK: $var" || echo "MISSING: $var"; done</automated>
  </verify>
  <done>All 8 spot-checked variables are present in .env.template. No literal secrets appear — all values are REDACTED or safe defaults.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → runtime | Committed files (docker-compose, Dockerfiles) must contain zero secrets; secrets flow only via .env at runtime |
| dashboard-sidecar env | LITELLM_MASTER_KEY must never appear here (SYS-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | docker-compose.yaml hardcoded DATABASE_URL | mitigate | Replace with ${POSTGRES_PASSWORD} variable — done in Task 1 |
| T-05-02 | Information Disclosure | .env.template missing LITELLM_MASTER_KEY entry | mitigate | Add entry with REDACTED placeholder so operator knows it is required — Task 2 |
| T-05-03 | Elevation of Privilege | LITELLM_MASTER_KEY accidentally added to dashboard-sidecar | mitigate | Task 1 explicitly instructs NOT to add it; comment in docker-compose references SYS-02 |
</threat_model>

<verification>
After both tasks complete:

```bash
# 1. No hardcoded password in compose file
grep "litellm-synergy-2026" docker-compose.yaml && echo FAIL || echo PASS

# 2. Template has all required vars
for v in POSTGRES_PASSWORD LITELLM_MASTER_KEY LITELLM_BENCH_KEY ANTHROPIC_API_KEY; do
  grep -q "^${v}=" .env.template && echo "OK $v" || echo "MISSING $v"
done

# 3. SYS-02: master key not in sidecar env block
python3 -c "
import yaml, sys
c = yaml.safe_load(open('docker-compose.yaml'))
sidecar_env = c['services']['dashboard-sidecar'].get('environment', [])
env_str = str(sidecar_env)
if 'LITELLM_MASTER_KEY' in env_str:
    print('FAIL: master key in sidecar env'); sys.exit(1)
else:
    print('PASS: master key absent from sidecar')
"
```
</verification>

<success_criteria>
- `grep "litellm-synergy-2026" docker-compose.yaml` returns no output
- `.env.template` contains entries for POSTGRES_PASSWORD, LITELLM_MASTER_KEY, LITELLM_BENCH_KEY, and all external API keys
- dashboard-sidecar environment block does not contain LITELLM_MASTER_KEY
- docker-compose.yaml is valid YAML (passes `docker compose config --quiet`)
</success_criteria>

<output>
After completion, create `.planning/phases/05-containerized-deployment/05-01-SUMMARY.md`
</output>
